{% extends 'base.back.html.twig' %}

{% block body %}
<br/>
<br/>
<section class="py-5">
<div class="container">
    <div class="w3_login_module" id="kk">
    <div class="module form-module">
        <div class="toggle">

        </div>
        <div class="form" >
       {{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
            {{ form_row(form.name) }}
            {{ form_row(form.age) }} <br>
            {{ form_end(form)}}
        </div>
        </div>
        <a href="/guide">Retour</a>
    </div>
</section>
{% endblock %}